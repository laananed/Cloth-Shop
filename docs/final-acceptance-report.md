# Cloth-Shop 最终验收报告

- 验收阶段：阶段 17
- 验收日期：2026-07-15
- 起始分支：`master`
- 起始 HEAD / `origin/master`：`73bd07effa932cc467d39b73c224ba3c1119d5e4`
- 最终提交名称：`chore: 完成项目最终审计与全量回归`
- 最终提交 SHA：本报告与阶段 17 变更位于同一提交，以 `git log -1` 为准
- 审计数据库命名规则：`frieren_cloth_shop_audit_<YYYYMMDD_HHMMSS>`

本报告不记录管理员密码、支付密码、数据库密码、Token 或 Authorization 内容。

## 1. 最终结论

项目可以在 MySQL 8.0.28 上按 01～10 从零初始化，可以启动前后端并完成课程设计范围内的前台购物、后台管理、数据库对象展示和操作日志演示。阶段 17 修复三个真实交付缺陷，并完成隔离数据库、API、浏览器和数据清理验收。

适用边界仍是课程设计系统，不等同于生产商城；并发压力、数据库故障注入、跨浏览器矩阵和真实支付网关不在本次验收范围。

## 2. Git 与仓库基线

- 初始工作区干净，分支为 `master`，没有进行中的 merge、rebase、cherry-pick 或 revert。
- 初始 `HEAD == origin/master == 73bd07e`，双向提交范围为空。
- `f36127e`、`0af4207`、`a9b43ce`、`029afc7`、`f821741`、`73bd07e` 均为当前历史祖先，未 squash、改写或重建。
- `Cloth-Shop-tags`、`Cloth-Shop-logs` 及对应远端分支和 Worktree 全部保留。
- 未执行 force push、rebase、amend、reset、worktree remove 或功能分支删除。

## 3. 环境版本

| 组件 | 版本 |
|---|---|
| MySQL | 8.0.28 |
| Node.js | 22.18.0 |
| npm | 10.9.3 |
| Python | 3.13.5 |
| Git | 2.54.0.windows.1 |

## 4. 最终修复

1. `04_测试数据与验证.sql`：03 的订单插入触发器会先生成状态日志，随后 04 固定 ID 的完整演示历史发生主键冲突。现在在显式历史写入前仅清理该批触发器临时日志并重置自增。
2. `06_商品描述增量迁移.sql`：缺少 `USE frieren_cloth_shop_db`，独立按编号执行时报“未选择数据库”。现在与 07～10 一致显式选择目标库。
3. `backend/requirements.txt`：代码使用 multipart 表单/文件路由，但依赖清单缺少 `python-multipart`。现在已声明并由回归测试锁定。
4. 仓库卫生：SQL SHA256 清单扩展到 01～10；运行时上传目录加入 `.gitignore`，不影响已有已跟踪演示图片。

未新增 SQL 11，未新增普通业务功能，未改变端口、技术栈、认证方式或正式开发数据。

## 5. SQL 01～10 审计

| 顺序 | 文件 | 首轮 | 重复执行/幂等性 | 数据影响与风险 |
|---|---|---|---|---|
| 01 | 数据库结构与增量迁移 | 退出 0 | 基础表不是全量幂等；仅用于新库 | 创建数据库、16 个基础表并补多图/复杂 SKU 结构 |
| 02 | 视图 | 退出 0 | 可重复，先删除后重建 | 重建 6 个视图，不清空业务表 |
| 03 | 过程/触发器/函数 | 退出 0 | 可重复，先删除后重建 | 重建 13 个过程、3 个触发器、1 个函数 |
| 04 | 测试数据与验证 | 退出 0 | 仅测试库可重复 | 会清空并重建固定演示数据，禁止在已有业务库执行 |
| 05 | 账号与支付密码初始化 | 退出 0 | 幂等 | 初始化测试账号字段和固定测试账号数据 |
| 06 | 商品描述增量迁移 | 退出 0 | 额外连续两轮均退出 0 | 兼容新增字段并覆盖商品视图，不清空业务数据 |
| 07 | 订单买家备注增量迁移 | 退出 0 | 额外连续两轮均退出 0 | 兼容新增字段、视图和过程 |
| 08 | 购物车选中项备注下单增量迁移 | 退出 0 | 额外连续两轮均退出 0 | 重建兼容过程，不改已有业务数据 |
| 09 | 商品多标签增量迁移 | 退出 0 | 额外连续两轮均退出 0 | `CREATE TABLE IF NOT EXISTS`，不清空标签/关系 |
| 10 | 管理员操作日志增量迁移 | 退出 0 | 额外连续两轮均退出 0 | 信息架构判断字段/索引，保留历史日志 |

01 自己创建审计库并设置 `utf8mb4_general_ci`。不得预先用不同 collation 创建同名空库，否则存储过程局部字符串与表列可能出现 collation 混用；这属于审计驱动环境要求，不是仓库 SQL 缺陷。

## 6. 审计数据库对象

审计库 `frieren_cloth_shop_audit_20260715_221810` 的真实对象：

- 18 张表：`cart`、`cart_item`、`category`、`inventory`、`inventory_log`、`operation_log`、`order_item`、`order_main`、`order_status_log`、`payment_record`、`product`、`product_image`、`product_sales_stat`、`product_sku`、`product_tag`、`tag`、`user`、`user_address`。
- 6 个视图：`v_inventory_status`、`v_order_summary`、`v_product_detail`、`v_product_sales_rank`、`v_user_cart_detail`、`v_user_order_detail`。
- 13 个存储过程、1 个函数、3 个触发器。
- 19 个外键、48 个表级索引名称组合。
- 从零初始化完成后固定数据规模：用户 8、地址 8、分类 4、商品 16、SKU 32、库存 32、购物车 5、购物车项 8、订单 10、订单项 15、支付 9、库存日志 28、订单状态日志 18、操作日志 6；标签和商品标签初始均为 0。

## 7. 数据完整性

以下查询结果均为 0：孤立 SKU、孤立库存、孤立图片、孤立商品标签、孤立购物车项、孤立订单项、无效默认地址、多主图商品、负库存、非空重复 SKU code、重复分类名、重复标签名、重复商品标签、非法操作日志结果、本轮运行时空 request_id。

运行时日志最终为 SUCCESS 38、FAILURE 2；敏感关键词扫描 0 行。历史初始化日志允许 request_id 为 NULL。

## 8. 自动测试与静态验证

- `npm.cmd test`：220/220 通过，0 失败、0 跳过、0 todo。
- `tests.product_tags_batch_unit`：4/4 通过。
- 全部正式 `src/*.js`：`node --check` 通过。
- `backend/app/main.py`、`db.py`、`operation_logs.py`、`tests/product_tags_batch_unit.py`：`py_compile` 通过。
- OpenAPI：56 个方法/路径组合，56 个唯一组合，无重复。
- `git diff --check`：通过。

新增 5 项阶段 17 契约测试，分别锁定 multipart 依赖、04 状态日志顺序、06～10 独立数据库选择、01～10 SHA256 清单和运行时上传忽略规则。

## 9. API 验收矩阵

| 模块 | 已执行结果 |
|---|---|
| 公共查询 | 根路由、数据库检查、分类、标签、商品均 200 |
| 认证 | 未认证后台查询 401；错误登录 401；正确登录 200；令牌未输出 |
| 分类 | 新增、修改、删除、恢复及后台列表通过 |
| 标签 | 新增、修改、删除、恢复通过；关联商品后删除 409 |
| 商品 | multipart 新增、介绍修改、上下架、单商品标签、批量标签通过 |
| SKU/库存 | SKU 查询、新增、修改、库存更新、逻辑删除通过 |
| 图片 | 首图上传通过；唯一图片删除 400；追加第二图后删除第一图通过 |
| 地址 | 新增、设默认、恢复原默认、逻辑删除通过 |
| 购物车 | 查询、加入、改量、删除、再次加入通过 |
| 下单/支付 | 选中项下单 200；错误支付密码 400；正确支付 200 |
| 发货/退款 | 发货、取消发货、退款申请、同意退款全部 200 |
| 直接购买 | 创建待支付订单并取消通过 |
| 订单/日志 | 用户订单、公开详情、后台详情、SUCCESS/FAILURE 筛选和选项查询均 200 |

未执行：令牌自然过期等待、重复支付、数据库断连/死锁注入、并发竞争压力。

## 10. 浏览器验收

### 桌面端

- 前台真实加载审计商品、分类和标签；文档宽度与滚动宽度一致，无横向溢出。
- 后台登录成功，7 个导航目标唯一；订单列表、操作日志、20 条分页和结构化详情展开正常。
- 单图商品从购买弹窗打开 lightbox，计数为 `1 / 1`。
- 前后台控制台错误均为 0。

### 390px

- 视口设为 390×844，浏览器内容宽度 375；前后台 `scrollWidth == clientWidth`，无横向溢出。
- 前台审计商品、6 个分类按钮、2 个标签按钮和唯一 lightbox 均存在。
- 后台保持登录状态，7 个导航均存在。
- 前后台控制台错误均为 0。

浏览器测试使用单一 Chromium 内核环境，未执行 Firefox、Safari、旧版 Edge 或真实移动设备矩阵。

## 11. 安全检查

- 管理员写接口继续要求 Bearer 认证，未认证返回 401。
- 后端动态用户输入使用参数化 SQL；动态占位符由已校验整数 ID 数量生成，排序字段使用固定白名单。
- 前端普通文本渲染沿用转义/文本节点路径；商品描述和日志详情未改为不安全 HTML。
- 上传校验覆盖格式、大小、空文件及事务失败清理；长期磁盘孤儿治理仍是已知边界。
- 操作日志白名单过滤密码、令牌、Authorization、数据库密码和文件内容；本轮敏感扫描为 0。
- `.env`、虚拟环境、缓存、日志、数据库备份、截图和临时脚本均未加入提交。
- 仍有课程设计级风险：普通用户交易接口没有用户 Token；管理员 Token 密钥存在源码默认值；测试账号是固定演示账号；不适合公网生产部署。

## 12. 仓库卫生

- Git 冲突标记扫描无残留。
- 未发现被跟踪的 `.env`、虚拟环境、缓存、日志、数据库备份、SQLite、dump、zip 或截图。
- `backend/uploads/` 新增忽略规则；既有正式演示图片仍保持跟踪。
- SQL 清单已同步 01～10 当前字节数与 SHA256。
- 正式文档未加入本机绝对路径、审计凭据或 Worktree 临时链接。

## 13. 数据恢复与清理

- 临时分类、标签、商品、SKU、图片、地址、购物车、订单和操作日志均位于审计库。
- 两个临时上传文件按数据库 URL 精确删除。
- 审计库已删除，`information_schema` 存在数为 0。
- 浏览器临时视口已恢复，验收标签页已关闭。
- 临时后端日志目录已删除。
- 原 8050 开发后端已恢复并确认连接 `frieren_cloth_shop_db`；5900 前端未中断。
- 真实开发库只做基线查询，未执行 04、未重建、未清空。

## 14. README 与架构文档

- README 已同步阶段 17 状态、220 项测试基线和最终报告入口。
- SQL 整理说明已同步 01～10 顺序、危险边界和幂等规则。
- 当前架构文档已同步起始 commit、56 条路由、依赖/上传修复、最终测试与阶段完成度。
- 本报告可作为答辩前最终检查清单和验收证据摘要。

## 15. 答辩演示建议顺序（10～15 分钟）

1. README 启动与 SQL 01～10 顺序，强调 04 只能用于测试库。
2. 前台分类、标签、关键词组合筛选。
3. 商品详情、多图片/lightbox、SKU 和库存状态。
4. 购物车选中项下单、错误支付密码重试、正确支付。
5. 后台分类、标签、商品、SKU、库存和图片。
6. 批量标签与单商品标签。
7. 订单发货、取消发货、退款申请与审核。
8. 操作日志筛选、分页、SUCCESS/FAILURE 和结构化详情。
9. 展示数据库 18 表、6 视图、13 过程、1 函数和 3 触发器。
10. 说明事务、库存锁、逻辑删除、已知课程设计边界和最终测试证据。

## 16. 未执行项与剩余风险

- 未执行高并发压力、长事务竞争、死锁重试和数据库故障注入。
- 未执行大批量商品/订单容量测试。
- 未执行真实支付网关、邮件/短信、生产密钥轮换和公网安全测试。
- 未执行多浏览器、多操作系统和真实移动设备兼容矩阵。
- 未制作学校模板课程报告、PPT 和讲解视频；本报告可作为这些材料的事实来源。

## 17. 阶段状态

阶段 17 完成后项目停止新增普通功能阶段。代码、SQL、文档、自动测试、隔离数据库、API、浏览器、数据清理和 Git 发布状态共同构成最终交付门禁。
